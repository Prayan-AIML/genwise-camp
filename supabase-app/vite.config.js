import { defineConfig } from 'vite'

// base: './' makes the built site work no matter what sub-path it's hosted at
// (e.g. GitHub Pages under /genwise-camp/).
export default defineConfig({
  base: './',
})
